// The mobile client is a single-conversation surface. Kept as a null export
// temporarily so stale imports fail closed instead of resurrecting tab chrome.
export function BottomNav() {
  return null;
}
