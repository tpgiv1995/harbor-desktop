# Electron app

This scaffold uses the system Node.js installation. It was created with Node.js v22.22.1.

Run `npm install`, then use `npm run dev` for development, `npm run build` to build the renderer, or `npm start` to launch the built app.
